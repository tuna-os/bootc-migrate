self-hosted-runner:
  # Labels of self-hosted runners the E2E workflows can target. The "kvm" label
  # is the KVM-capable host (kanpur); it is opt-in via vars.E2E_SELF_HOSTED now
  # that the matrix defaults to GitHub-hosted runners.
  labels:
    - kvm
